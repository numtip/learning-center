# Copy to .env.docker (gitignored) before running docker compose.
# Used for Compose variable substitution only — not loaded by Laravel.

COMPOSE_PROJECT_NAME=learning-center

MYSQL_DATABASE=learning_center
MYSQL_USER=learning_center
MYSQL_PASSWORD=learning_center_local_dev
MYSQL_ROOT_PASSWORD=learning_center_root_local_dev
